<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>$!{pageTitle} - PVLens</title>

    <!-- Google Fonts - Outfit -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Main Stylesheet -->
    <link rel="stylesheet" href="$link.setPage('css/pvlens.css')">

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="$link.setPage('images/favicon.svg')">
</head>
<body>
    <div class="app-container">
        <!-- Header Navigation -->
        <header class="header">
            <div class="header-left">
                <a href="$link.setScreen('Overview')" class="logo">
                    <svg class="logo-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <rect width="40" height="40" rx="12" fill="#76FB91"/>
                        <path d="M20 10L28 18L20 26L12 18L20 10Z" fill="#000"/>
                        <circle cx="20" cy="18" r="3" fill="#76FB91"/>
                    </svg>
                    <span class="logo-text">PVLens</span>
                </a>

                <nav class="main-nav">
                    <a href="$link.setScreen('Overview')" class="nav-link #if($currentScreen == 'Overview')active#end">
                        Overview
                    </a>
                    <a href="$link.setScreen('Substances')" class="nav-link #if($currentScreen == 'Substances' || $currentScreen == 'SubstanceDetail')active#end">
                        Substances
                    </a>
                    <a href="$link.setScreen('AdverseEvents')" class="nav-link #if($currentScreen == 'AdverseEvents')active#end">
                        Adverse Events
                    </a>
                    <a href="$link.setScreen('Indications')" class="nav-link #if($currentScreen == 'Indications')active#end">
                        Indications
                    </a>
                </nav>
            </div>
        </header>

        <!-- Main Content -->
        <main class="main-content">
            $screen_placeholder
        </main>

        <!-- Footer -->
        <footer class="footer">
            <p>PVLens - Pharmacovigilance Dashboard | <a href="https://github.com/GSK-Global-Safety/pvlens" target="_blank">Source</a></p>
        </footer>
    </div>

    <!-- JavaScript -->
    <script src="$link.setPage('js/pvlens.js')?v=3"></script>
</body>
</html>
